<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.produtividade">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EntradaDoisActivity"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DiasActivity" />
        <activity android:name=".QuandoBloquearActivity" />
        <activity android:name=".HorasBloqueioActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity
            android:name=".PasscodeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".AlarmActivity" />

        <receiver android:name=".AlarmReceiver" />

        <activity android:name=".ResetSenhaActivity" />
        <activity
            android:name=".ResetPasscodeActivity"
            android:theme="@style/AppThemeNoActionBar" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".BemVindoActivity" />
        <activity android:name=".InfoActivity"></activity>
    </application>

</manifest>